# Development Dockerfile with Vite hot-reloading
FROM node:22.12.0-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the entire application
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Run Vite dev server (host is configured in vite.config.ts)
CMD ["npm", "run", "dev"]

