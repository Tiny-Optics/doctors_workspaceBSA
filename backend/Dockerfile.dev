# Development Dockerfile with Air for hot-reloading
FROM golang:1.24.4-alpine

# Install build dependencies (make is required by Air config)
RUN apk add --no-cache make

# Install air for live reloading (v1.61.1 is compatible with Go 1.24)
RUN go install github.com/air-verse/air@v1.61.1

# Set working directory
WORKDIR /app

# Copy go mod files
COPY go.mod go.sum ./

# Download dependencies
RUN go mod download

# Copy the entire application
COPY . .

# Build the application initially
RUN go build -o main cmd/api/main.go

# Expose the application port
EXPOSE 8080

# Run air for hot-reloading
CMD ["air"]


